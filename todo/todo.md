* It's not possible to choose different modes of easing in the CameraAnimator. Could be nice with acouple of alterantives
* Could be nice with more operators for the spatial layout, for example hidden() so that you can hide a slide before it is activated, hiddenAfter, hiddenBefore
* Setup demo at github pages
* Go through the example presentations and check that the texts are relevant. Also move them to shared examples.
* Create a terminal that let the user choose which example to execute
* Some of the transitions in SharedSimpleExamplesPresentation are still not completed when everything has changed